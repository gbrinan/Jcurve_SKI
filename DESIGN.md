# DESIGN.md — slide-pack SK이노베이션 CI 테마

> [awesome-design-md](https://github.com/VoltAgent/awesome-design-md) 철학을 따르는 디자인 시스템 문서.
> AI 에이전트는 슬라이드를 생성할 때 이 파일을 읽고 아래 토큰과 규칙만 사용한다.
> 이 파일이 디자인의 SSOT다 — 색·폰트를 슬라이드 HTML에 직접 하드코딩해 늘리지 말 것.

## 1. 비주얼 테마 & 분위기

- 밝고 신뢰감 있는 **기업 발표용** 룩. 흰 배경 위에 SK 레드/오렌지 포인트.
- 화려한 효과보다 **여백과 위계**로 정돈. 슬라이드당 시선의 목적지는 1곳.
- 애니메이션·그라데이션·장식 아이콘 금지 (제거 우선).

## 2. 컬러 팔레트 (시맨틱 토큰)

| 토큰 | 값 | 역할 |
|---|---|---|
| `--brand` | `#EA002C` | SK 레드 — 키커, 강조 단어, 구분선. 한 슬라이드에 2곳 이하 |
| `--brand-sub` | `#F47725` | SK 오렌지 — 보조 강조, 표 헤더, 불릿 마커 |
| `--bg` | `#FFFFFF` | 슬라이드 배경 |
| `--bg-soft` | `#F7F5F2` | 코드 블록·인용 배경 |
| `--text` | `#1A1A1A` | 본문 |
| `--muted` | `#6E6E6E` | 부제·캡션 |
| `--line` | `#E3DFDA` | 표 괘선·테두리 |
| `--ok` | `#1E7A46` | 체크·완료 상태 |

> ⚠️ 위 값은 공개 자료 기반 근사치다. 사내 브랜드 가이드라인의 공식 CI 색상값을 확보하면
> **이 표만 교체**한다 — 슬라이드 HTML은 토큰만 참조하므로 자동 반영된다.

## 3. 타이포그래피

| 위계 | 폰트 | 크기(16:9 기준) | 굵기 |
|---|---|---|---|
| 타이틀(h1) | Pretendard → Malgun Gothic 폴백 | clamp(30px, 6.5vh, 60px) | 800 |
| 섹션 제목(h2) | 동일 | clamp(22px, 4.4vh, 40px) | 700 |
| 본문·불릿 | 동일 | clamp(14px, 2.4vh, 22px) | 400 |
| 키커(말머리) | 동일 | clamp(12px, 1.6vh, 16px), 자간 .12em | 700, `--brand` |
| 캡션 | 동일 | clamp(12px, 1.9vh, 16px) | 400, `--muted` |

- 사내 전용 서체(예: SK그룹체)가 파일로 제공되면 self-contained 원칙에 따라
  **base64 서브셋으로 인라인**하고 첫 번째 폴백으로 둔다. 외부 폰트 URL 금지.

## 4. 컴포넌트 규칙

- **키커**: 모든 슬라이드 상단, 대문자/자간 스타일, `--brand`.
- **불릿**: `▪` 마커 `--brand-sub`. 슬라이드당 최대 5개, 1줄씩.
- **표**: 헤더 텍스트 `--brand-sub`, 헤더 하단 2px `--brand` 선. 최대 6행.
- **강조(strong)**: `--brand`. 한 문장에 1개까지.
- **섹션 구분 슬라이드**: `--brand` 배경 + 흰 텍스트, 제목만 배치. 파트가 3개 이상일 때만 사용.

## 5. 레이아웃

- 16:9, 패딩 clamp(24px, 5vh, 56px) / clamp(28px, 6vw, 72px).
- 세로 중앙 정렬, 요소 간 gap clamp(12px, 2.5vh, 26px).
- 좌측 정렬 기본. 가운데 정렬은 타이틀·섹션 구분 슬라이드만.

## 6. 가드레일 (Do / Don't)

- ✅ 색은 이 파일의 토큰만 사용한다.
- ✅ 레드는 강조용이다 — 본문을 레드로 쓰지 않는다.
- ✅ 다크 테마 덱(기본 템플릿)과 CI 테마 덱을 한 파일에 섞지 않는다.
- ❌ 로고 이미지·외부 리소스 삽입 금지 (self-contained 위반 + 상표 관리 이슈).
- ❌ 새 색·새 폰트 크기 즉석 추가 금지. 필요하면 이 파일을 먼저 수정한다.

## 7. 에이전트 프롬프트 (복사용)

```
DESIGN.md를 읽고, 그 토큰과 규칙만 사용해서
assets/template-sk-ci.html 스켈레톤 위에 슬라이드를 생성하라.
DESIGN.md에 없는 색·크기가 필요하면 임의로 만들지 말고 나에게 물어라.
```
