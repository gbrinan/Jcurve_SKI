---
name: slide-pack
version: "1.0.0"
description: "기획서·프롬프트·마크다운 문서를 팀 공유용 HTML 슬라이드 덱(Codex 슬라이드 포맷)으로 시각화한다. 팀원에게 결과물을 깔끔하게 발표·공유해야 할 때, '슬라이드로 만들어줘', '발표용으로 정리해줘', '패키징 결과를 보여주자' 같은 요청에 사용한다."
argument-hint: 'slide-pack agent-plan.md | slide-pack 이 폴더의 기획서를 슬라이드로'
allowed-tools: Read, Write, Glob
user-invocable: true
---

# slide-pack — 팀 공유용 HTML 슬라이드 패키징 스킬

문서(기획서, 프롬프트, README, 인터뷰 확정 요약 등)를 받아
브라우저에서 바로 열리는 **자기완결형(self-contained) HTML 슬라이드 덱**으로 변환한다.
설계 철학은 paperthin의 제거 우선: 문서를 그대로 옮기지 말고, 발표에 필요한 것만 남긴다.

## Codex 슬라이드 포맷 규칙

산출물은 HTML 파일 **1개**이며 다음을 지킨다:

1. **자기완결**: 외부 CDN·폰트·이미지 요청 금지. 모든 CSS/JS는 인라인. 파일 하나를 드라이브에 올리면 누구나 더블클릭으로 열 수 있어야 한다.
2. **구조**: 슬라이드 1장 = `<section class="slide">` 1개. 16:9 비율, 화면 중앙 정렬.
3. **네비게이션**: 템플릿이 제공하는 것을 그대로 쓴다 — 직접 만들지 않는다.
   - 이동: ←/→ · 스페이스 · 좌우 가장자리 화살표 존 · 터치 스와이프 · 숫자키(1~9) 직접 점프
   - **본문 클릭으로는 넘어가지 않는다** — 표·텍스트를 선택해 복사할 수 있어야 하기 때문이다
   - 하단 바: 좌측 조작 힌트 · 중앙 **도트 페이지네이션**(호버 시 슬라이드 제목, 클릭 시 점프) · 우측 `현재/전체`
   - 상단 진행률 바, 이동 방향에 따른 전환 애니메이션(`prefers-reduced-motion` 존중)
   - **목차 패널**: `O` 키로 열고 `Esc`로 닫는다. 슬라이드 제목 목록에서 클릭해 점프
   - **딥링크**: 현재 장이 URL 해시(`#s4`)에 반영된다. `덱.html#s4`로 특정 장을 바로 공유할 수 있다
4. **밀도 제한 (제거 우선)**:
   - 슬라이드당 핵심 메시지 1개, 불릿 최대 5개, 불릿당 1줄
   - 표는 최대 6행 — 넘치면 "대표 N건 + 전체는 원문 참조"로 줄이고, 마지막 SSOT 슬라이드 번호를 함께 적는다 (예: "전체 12건은 원문 참조 — 9장")
   - 원문의 문장을 복사하지 말고 키워드로 압축한다
5. **SSOT 표시**: 마지막 슬라이드에 원문 문서의 위치(파일명·경로)를 명시한다. 슬라이드는 요약본이며 원본이 아니다 — 원문이 바뀌면 슬라이드를 다시 생성한다.
6. **템플릿**: 색·폰트·네비게이션 JS를 재발명하지 않는다. 용도에 따라 선택:
   - 팀 내부 공유(기본): `assets/template.html` (다크)
   - 발표용(SK CI): `assets/template-sk-ci.html` — 디자인 토큰의 SSOT는 `DESIGN.md`이며, DESIGN.md에 없는 색·크기는 임의로 만들지 않는다
7. **발표용 추가 규칙** (SK CI 테마일 때):
   - 슬라이드 제목은 주제가 아니라 **주장(한 문장 메시지)**으로 쓴다
   - 파트가 3개 이상이면 섹션 구분 슬라이드(`slide divider`)를 넣는다
   - 첫 장에 발표자·역할 분담, 마지막 장에 원문(SSOT) 위치를 넣는다

## 변환 절차

1. 입력 문서를 읽고 섹션 구조를 파악한다.
2. 슬라이드 개요를 먼저 뽑는다: 타이틀 1장 + 섹션당 1장 + 마무리(원문 위치) 1장. **10장을 넘기지 않는다** — 넘치면 섹션을 병합하거나 뺀다.
3. 각 슬라이드를 밀도 제한에 맞게 압축한다. 뺀 내용은 마지막 슬라이드에 "원문에서 확인"으로 안내한다.
4. `assets/template.html`을 읽어 `<!-- SLIDES -->` 위치에 슬라이드를 채우고, `<!-- TITLE -->`을 제목으로 치환해 저장한다.
5. 생성 후 자기 점검: 외부 URL 참조가 없는가, 페이지 표시가 전체 장수와 맞는가, 원문 위치 슬라이드가 있는가,
   **모든 슬라이드에 `.kicker`가 있는가** — 도트 툴팁과 목차 패널이 이 값을 슬라이드 이름으로 쓴다. 없으면 h1/h2로 대체되지만 목록이 길어져 읽기 어렵다.

## 산출물 이름 규칙

`<원문파일명>-deck.html` (예: `agent-plan.md` → `agent-plan-deck.html`).
원문과 같은 폴더에 저장한다.
